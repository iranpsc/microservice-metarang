{{define "email/password_reset"}}
{{template "base" .}}
{{end}}

{{define "email/password_reset/content"}}
  <h1>تغییر گذرواژه</h1>
  <p>
    {{with .RecipientName}}سلام {{.}}،{{else}}سلام شهروند عزیز،{{end}}
    درخواست تغییر گذرواژه برای حساب کاربری متارنگ ثبت شده است. برای ادامه مسیر روی دکمه زیر کلیک کن.
  </p>

  {{with .ResetURL}}
  <div style="text-align: center; margin: 24px 0;">
    <a href="{{.}}" class="button">تغییر گذرواژه</a>
  </div>
  <p style="word-break: break-all; text-align:center;">
    اگر دکمه برایت قابل مشاهده نیست، از لینک زیر استفاده کن:<br>
    <a href="{{.}}" style="color:#fd7e14;">{{.}}</a>
  </p>
  {{end}}

  <table class="details-table" role="presentation">
    <tbody>
      <tr>
        <th>شناسه شهروندی</th>
        <td>{{with .UserCode}}{{.}}{{else}}نامشخص{{end}}</td>
      </tr>
      {{with .RequestedAt}}
      <tr>
        <th>زمان درخواست</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .RequestIP}}
      <tr>
        <th>آی‌پی درخواست</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      <tr>
        <th>اعتبار لینک</th>
        <td>{{with .ExpiresIn}}{{.}}{{else}}۶۰ دقیقه{{end}}</td>
      </tr>
    </tbody>
  </table>

  {{with .DeclineURL}}
    <div style="text-align: center; margin-top: 16px;">
      <a href="{{.}}" class="button" style="background-color:#3d1554; border:2px solid #fd7e14;">این درخواست من نیست</a>
    </div>
  {{end}}

  <p class="muted" style="margin-top:24px;">
    اگر تغییر گذرواژه را آغاز نکرده‌ای، بلافاصله با پشتیبانی متارنگ تماس بگیر تا حسابت بررسی شود.
  </p>
{{end}}


