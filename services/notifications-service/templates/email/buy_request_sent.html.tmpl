{{define "email/buy_request_sent"}}
{{template "base" .}}
{{end}}

{{define "email/buy_request_sent/content"}}
  <h1>درخواست خرید ارسال شد</h1>
  <p>
    {{with .BuyerName}}سلام {{.}}،{{else}}سلام شهروند عزیز،{{end}}
    پیشنهاد خرید VOD برای مالک دارایی ارسال شد. خلاصه اطلاعات پیشنهادت را در جدول زیر ببین.
  </p>

  <table class="details-table" role="presentation">
    <tbody>
      <tr>
        <th>شناسه خریدار</th>
        <td>{{with .BuyerCode}}{{.}}{{else}}—{{end}}</td>
      </tr>
      <tr>
        <th>شناسه VOD</th>
        <td>{{with .FeatureID}}{{.}}{{else}}—{{end}}</td>
      </tr>
      <tr>
        <th>متراژ</th>
        <td>{{with .FeatureArea}}{{.}}{{else}}—{{end}}</td>
      </tr>
      <tr>
        <th>کاربری</th>
        <td>{{with .FeatureApplication}}{{.}}{{else}}—{{end}}</td>
      </tr>
      <tr>
        <th>تراکم</th>
        <td>{{with .FeatureDensity}}{{.}}{{else}}—{{end}}</td>
      </tr>
      {{with .FeatureCoordinates}}
      <tr>
        <th>مرزبندی</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .FeatureAddress}}
      <tr>
        <th>آدرس</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      <tr>
        <th>پیشنهاد IRR</th>
        <td>{{with .OfferIRR}}{{.}}{{else}}—{{end}}</td>
      </tr>
      <tr>
        <th>پیشنهاد PSC</th>
        <td>{{with .OfferPSC}}{{.}}{{else}}—{{end}}</td>
      </tr>
      {{with .RequestId}}
      <tr>
        <th>شناسه پیشنهاد</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .CreatedDate}}
      <tr>
        <th>تاریخ ارسال</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .CreatedTime}}
      <tr>
        <th>ساعت ارسال</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
    </tbody>
  </table>

  <p>
    اگر این پیشنهاد توسط تو ثبت نشده است، می‌توانی آن را فوراً لغو کنی تا دسترسی مشکوک بررسی شود.
  </p>

  <div style="text-align:center; margin: 24px 0;">
    {{with .CancelURL}}
      <a href="{{.}}" class="button" style="background-color:#3d1554; border:2px solid #fd7e14;">لغو پیشنهاد</a>
    {{end}}
    {{with .SecurityURL}}
      <a href="{{.}}" class="button" style="background-color:#3d1554; border:2px solid #795E8B;">پروتکل‌های امنیتی</a>
    {{end}}
  </div>

  <p class="muted">
    برای پیگیری بیشتر با <a href="{{with .ContactURL}}{{.}}{{else}}https://rgb.irpsc.com/contact{{end}}" style="color:#b0a7b7;">پشتیبانی متارنگ</a> در ارتباط باش یا پرسش‌هایت را در
    {{with .FaqURL}}<a href="{{.}}" style="color:#b0a7b7;">انجمن متاورس</a>{{else}}انجمن متاورس{{end}} مطرح کن.
  </p>
{{end}}


